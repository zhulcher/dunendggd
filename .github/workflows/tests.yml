name: Automated tests

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  test:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Setup gegede
      run: pip install -e git+ssh://git@github.com/brettviren/gegede.git@86ca28190516a23203cd883aafb0548a61664ceb#egg=gegede
    - name: Setup
      run: pip install .
    - name: Build defaul gdml
      run: ./build_hall.sh
